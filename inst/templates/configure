# In configure scripts, Rscript cannot be used without path specified
RSCRIPT="${R_HOME}/bin/Rscript"

# Load the variables to customize the checks (e.g. minimum supported rust versions)
. ./tools/extendr_configure_settings.sh

# Load the function to check the Rust installation (e.g. check_cargo)
. ./tools/extendr_configure_functions.sh

# Even when `cargo` is on `PATH`, `rustc` might not. This should ensure PATH is
# configured correctly (c.f. https://github.com/yutannihilation/string2path/issues/4).
if [ -e "${HOME}/.cargo/env" ]; then
  . "${HOME}/.cargo/env"
fi

check_cargo
